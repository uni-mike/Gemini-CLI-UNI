{
  "permissions": {
    "allow": [
      "Bash(APPROVAL_MODE=yolo npx tsx src/cli.tsx --prompt \"Create a test file called test-output.txt with current date and git status, then read it back to verify\" --non-interactive)",
      "Bash(cat:*)",
      "Bash(APPROVAL_MODE=yolo npx tsx src/cli.tsx --prompt \"Find TODO comments in TypeScript files using grep, then use cat to preview the README.md file\" --non-interactive)"
    ],
    "deny": [],
    "ask": []
  }
}